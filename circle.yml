# https://circleci.com/docs/configuration/

machine:
  timezone: Asia/Tokyo
  node:
    version: 6.2.0

general:
  branches:
    ignore:
      - master

test:
  pre:
    - npm run build

deployment:
  production:
    branch: source
    commands:
      - npm run deploy
